Generate Q&A pairs from search results, ensuring that the language in the Q&A pairs matches that of the search results. Create a single JSON file with appropriate formatting and indentation. Always leave "text_content_ empty. If you got more than one query, only treat and return result for the first one. Always return a SINGLE JSON file.